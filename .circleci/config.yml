version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Pod Spec Update"
          command: "bash spec_update.sh"
      - run:
          name: "Echo OK"
          command: "echo ok"

  gr:
    docker:
      - image: cimg/base:current
    steps:
      - checkout

  gr2:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Echo Gr2"
          command: "echo Gr2"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - gr:
          requires:
            - say-hello
      - gr2:
          requires:
            - say-hello
